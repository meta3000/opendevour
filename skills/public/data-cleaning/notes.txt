数据清洗注意事项
================

1. 清洗前务必备份原始数据（脚本会生成 .bak）。
2. 编码统一转为 UTF-8，避免中文乱码。
3. 去重以整行为单位，若只需按主键去重需另行处理。
4. 时间字段统一为 ISO-8601（YYYY-MM-DD）。
5. 金额字段去除千分位逗号后再入库。

本文件为纯文本（.txt），在 Skills 工作区中以无高亮的纯文本方式展示。
