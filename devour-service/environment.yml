name: opendevour
dependencies:
  # --- 基础 ---
  - python=3.13
  - pip
  # --- Web 框架 ---
  - fastapi>=0.115
  - uvicorn>=0.32
  - pydantic>=2.9
  - pydantic-settings>=2.6
  - sse-starlette>=2.1
  - python-dotenv>=1.0
  - httpx>=0.27
  # --- 数据库 ---
  - sqlalchemy>=2.0
  - aiosqlite>=0.20
  # --- 数据科学 ---
  - pandas>=2.2
  - numpy>=2.0
  # --- AI/Agent（conda-forge 版本不兼容，走 pip）---
  - cachetools>=5.5
  # --- 调度 ---
  - apscheduler>=3.10,<4
  # --- pip 安装（conda-forge 版本过旧或不兼容）---
  - pip:
    - langchain>=1.3
    - langchain-openai>=0.2
    - langgraph>=1.2
    - tushare>=1.4.29
